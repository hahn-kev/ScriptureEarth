name: Preview cleanup

# Scheduled prune of stale Cloudflare Pages *preview* deployments for the
# se-proto-en project. preview.yml creates one preview deployment per push on
# every PR branch and never deletes them, so they accumulate forever. This job
# lists all deployments and deletes preview-environment ones older than 30 days.
#
# Production deployments are never touched (environment filter). Deployments that
# are still aliased (e.g. the current head of an open PR branch) are refused by
# the API without ?force=true, which we deliberately do not pass — so a live
# branch alias is preserved even if it is older than 30 days, and only truly
# stale, superseded preview builds are pruned.
#
# The job self-gates: until the Cloudflare secrets are configured it skips
# (green) with a notice. Required configuration:
#   secrets.CLOUDFLARE_API_TOKEN   token with Account · Cloudflare Pages · Edit
#   secrets.CLOUDFLARE_ACCOUNT_ID  Cloudflare account id

on:
  schedule:
    # 07:00 UTC every Monday
    - cron: '0 7 * * 1'
  workflow_dispatch:
    inputs:
      max_age_days:
        description: 'Delete preview deployments older than this many days'
        required: false
        default: '30'

permissions:
  contents: read

concurrency:
  group: preview-cleanup
  cancel-in-progress: false

jobs:
  gate:
    runs-on: ubuntu-latest
    outputs:
      ready: ${{ steps.check.outputs.ready }}
    steps:
      - name: Check cleanup prerequisites
        id: check
        env:
          CF_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CF_ACCOUNT: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          if [ -n "$CF_TOKEN" ] && [ -n "$CF_ACCOUNT" ]; then
            echo "ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "ready=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Preview cleanup skipped — set secrets.CLOUDFLARE_API_TOKEN and secrets.CLOUDFLARE_ACCOUNT_ID to enable."
          fi

  prune:
    needs: gate
    if: needs.gate.outputs.ready == 'true'
    runs-on: ubuntu-latest
    env:
      CF_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CF_ACCOUNT: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      PROJECT: se-proto-en
      MAX_AGE_DAYS: ${{ github.event.inputs.max_age_days || '30' }}
    steps:
      - name: Prune stale preview deployments
        run: |
          set -euo pipefail

          api="https://api.cloudflare.com/client/v4/accounts/${CF_ACCOUNT}/pages/projects/${PROJECT}/deployments"
          auth="Authorization: Bearer ${CF_TOKEN}"
          cutoff=$(date -u -d "${MAX_AGE_DAYS} days ago" +%Y-%m-%dT%H:%M:%SZ)
          echo "Pruning preview deployments created before ${cutoff}"

          deleted=0 kept=0 skipped=0 page=1
          while :; do
            resp=$(curl -fsS -H "$auth" "${api}?per_page=100&page=${page}")
            if [ "$(printf '%s' "$resp" | jq -r '.success')" != "true" ]; then
              echo "::error::Cloudflare API listing failed"
              printf '%s\n' "$resp" | jq -r '.errors' >&2 || true
              exit 1
            fi

            count=$(printf '%s' "$resp" | jq '.result | length')
            [ "$count" -eq 0 ] && break

            # id + created_on for preview deployments older than the cutoff
            while IFS=$'\t' read -r id created; do
              [ -z "$id" ] && continue
              code=$(curl -s -o /tmp/del.json -w '%{http_code}' -X DELETE -H "$auth" "${api}/${id}")
              if [ "$code" = "200" ]; then
                echo "deleted ${id} (${created})"
                deleted=$((deleted + 1))
              else
                # Aliased/live deployments are refused without ?force=true — leave them.
                echo "skipped ${id} (${created}) — HTTP ${code}"
                skipped=$((skipped + 1))
              fi
            done < <(printf '%s' "$resp" | jq -r \
              --arg cutoff "$cutoff" \
              '.result[] | select(.environment == "preview" and .created_on < $cutoff) | "\(.id)\t\(.created_on)"')

            kept=$((kept + count))
            page=$((page + 1))
          done

          echo "Done. deleted=${deleted} skipped=${skipped} scanned=${kept}"
          {
            echo "### Cloudflare preview cleanup"
            echo ""
            echo "- Cutoff: \`${cutoff}\` (older than ${MAX_AGE_DAYS} days)"
            echo "- Deleted: ${deleted}"
            echo "- Skipped (aliased/live): ${skipped}"
          } >> "$GITHUB_STEP_SUMMARY"
