{
  "name": "scriptureearth-astro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ScriptureEarth public discovery site — static Astro rebuild (PoC)",
  "scripts": {
    "extract": "node scripts/extract.mjs && node scripts/build_search_index.mjs",
    "extract:api": "node scripts/harvest/project.mjs",
    "extract:i18n": "node scripts/extract_i18n.mjs",
    "gen:geo": "node scripts/gen_geo.mjs",
    "fetch:dump": "node scripts/fetch_dump.mjs",
    "convert:dump": "node scripts/convert_dump.mjs",
    "dev": "astro dev",
    "build": "astro build",
    "build:dump": "npm run extract && astro build",
    "build:api": "npm run extract:api && astro build",
    "build:fresh": "npm run fetch:dump && npm run convert:dump && npm run build:dump",
    "preview": "astro preview",
    "predeploy": "node scripts/build_sizes.mjs && node scripts/precompress_dist.mjs",
    "bench:size": "node scripts/build_sizes.mjs && node scripts/check_sizes.mjs",
    "deploy": "npx wrangler pages deploy dist --project-name se-proto-en --branch main",
    "deploy:api": "npm run build:api && npm run deploy",
    "deploy:dump": "npm run build:dump && npm run deploy",
    "deploy:fresh": "npm run build:fresh && npm run deploy",
    "bench:search-index": "node bench/harness.mjs"
  },
  "dependencies": {
    "astro": "^5.13.0",
    "minisearch": "^7.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.7",
    "msgpackr": "^2.0.5"
  }
}
